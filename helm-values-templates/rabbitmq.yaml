image:
  registry: ${DOCKER_REGISTRY_URL}

ingress:
  enabled: true
  host: rabbitmq.local

persistence:
  enabled: true
  existingClaim: rabbitmq-persistent-volume-claim

rabbitmq:
  password: passw0rd
  username: user

volumePermissions:
  enabled: true
